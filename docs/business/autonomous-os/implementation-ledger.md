# Autonomous OS implementation ledger

**Ledger version:** 1

**Established:** 2026-07-24

**Scope:** canonical G01–G20 capability gaps and their implementation gates

## Status vocabulary

| Status | Meaning |
|---|---|
| `Blocked` | A release or activation must not cross this gap. |
| `Planned` | Scope is defined; implementation evidence is not yet accepted. |
| `In progress` | Work exists on the implementation branch but the exit gate is not complete. |
| `Partial` | Some control exists, but it does not satisfy the full gate. |
| `Verified` | Evidence has been reviewed against the exit gate. |
| `Deferred` | Explicitly outside the current release and unable to block a narrower approved release. |

`Verified` requires evidence. A merged file, a passing unit test, or an operator assertion alone is not sufficient when the gate requires live reconciliation, delivery proof, or business outcomes.

The title, department, severity, maturity level, and priority below are canonical to the command-center capability-gap model. `Implementation` is the delivery state maintained by this ledger; it does not replace those canonical fields.

## Gap ledger

| ID | Canonical capability gap | Department | Canonical status | Implementation | Dependencies | Exit gate |
|---|---|---|---|---|---|---|
| G01 | No canonical owner decision queue | Executive | Critical · L-B · P0 | `In progress` | Canonical item schema; action protocol; reconciliation; SLA; target registry | All owner-decision surfaces read one uncapped durable queue; dismiss remains distinct from action; completion transactionally verifies the target record and records evidence. |
| G02 | Chief of Staff layer is absent | Executive | Critical · L-A · P3 | `In progress` | Two trustworthy department reports; goals; authority; G01; accepted handoffs | At least two supervised department loops produce accepted reports; a goal and dependency registry exists; the Chief of Staff is shadow-tested before any decision authority. |
| G03 | Green health does not mean business health | Platform Ops | Critical · L-B · P0 | `In progress` | Per-agent outcome contracts; quality evaluation; freshness; business KPIs | Health separately reports execution, output, quality, delivery, and outcome; a completed no-op cannot appear business-healthy. |
| G04 | Recovered incidents remain unresolved attention | Platform Ops | High · L-B · P0 | `In progress` | Shared incident ID; recovery transaction; reconciliation worker | Incident recovery closes or supersedes linked attention transactionally; a short-window reconciler proves no recovered incident remains falsely open. |
| G05 | Closed-won does not start onboarding | Sales | Critical · L-B · P1 | `In progress` | Authoritative customer identity; idempotent handoff; onboarding state machine | A closed-won event creates one acknowledged onboarding workflow with owner, due dates, evidence, retries, SLA, and exception routing. |
| G06 | Sales call state conflicts with orchestration output | Sales | High · L-B · P1 | `In progress` | Call-event schema; calendar linkage; preparation and follow-up state | Call requirements persist at record level and link event, preparation, owner, due date, completion, follow-up, and outcome in one authoritative workflow. |
| G07 | Task page is operational but has no managed work | Executive | Medium · L-C · P2 | `Planned` | Handoff/SLA model; producers; owners; outcome linkage; G01 decision | A recorded architecture decision either makes G01 the work ledger or connects tasks without duplication; generated tasks have owner, priority, SLA, completion, and outcome evidence. |
| G08 | Content pipeline completes without publication | Marketing | High · L-B · P2 | `In progress` | Delivery receipt; stuck-state SLO; retry; external reconciliation | Completed no-op is a distinct state; every claimed publication has an external delivery ID; stuck work retries or enters the owner queue with evidence. |
| G09 | Content quality depends on heavy owner review | Marketing | High · L-C · P2 | `In progress` | Rubric; evaluation store; sampled calibration; prompt/version lineage | Quality evaluations are versioned and calibrated to owner decisions; routine-class rejection remains below the approved threshold for the required observation window. |
| G10 | Marketing lacks outcome attribution | Marketing | High · L-A · P3 | `Deferred` | Identity lineage; campaign taxonomy; revenue attribution | Content and campaigns carry durable lineage through delivery, engagement, qualified lead, win, and reconciled revenue; attribution is tested before optimization authority. |
| G11 | Onboarding is a tracker, not an owned workflow | Onboarding | Critical · L-B · P1 | `Planned` | Authoritative identity; G05; workflow state machine; SLA | One authoritative seven-day workflow owns prerequisites, automated steps, human moments, blocks, SLAs, acceptance, and exception routing. |
| G12 | Client health is heuristic without intervention proof | Client Success | High · L-B · P2 | `In progress` | Backtesting; action plan; SLA; outcome labels | Health signals are backtested; every material risk creates an owned intervention plan; retention outcomes measure whether intervention changed the result. |
| G13 | No monthly client value-report workflow | Client Success | High · L-A · P3 | `Deferred` | Client identity; accepted attribution; approved value metrics | A recurring report instance is generated from evidence, reviewed, delivered, and acknowledged; delivery and client response are durable states. |
| G14 | Finance totals are not reconciled | Finance | Critical · L-B · P0 | `In progress` | Canonical ledger rules; period controls; reconciliation; sign-off | All finance surfaces use one calculation contract; source totals reconcile for a closed test period; differences block sign-off and route with evidence. |
| G15 | Paying-client and margin truth is incomplete | Finance | Critical · L-B · P1 | `In progress` | Authoritative billing mapping; customer master; entity attribution | Billing, customer identity, revenue, cost, and margin reconcile to authoritative sources; unattributed entries cannot produce authoritative client metrics. |
| G16 | Monthly close lacks an operating state machine | Finance | High · L-C · P2 | `In progress` | G14; close model; audit trail | Each period tracks reconciliations, exceptions, evidence, reviewer, lock, export, and sign-off; incomplete work cannot appear closed. |
| G17 | Admin role contract disagrees across tiers | Administration | High · L-B · P0 | `In progress` | Shared roles; metadata hygiene; positive and negative tests | One role/authority contract drives web, mobile, routing, and backend authorization; cross-tier integration tests prove matching allow and deny decisions. |
| G18 | Cross-tenant isolation is not runtime-proven | Administration | High · L-B · P0 | `In progress` | Seeded isolated tenants; CI identities; negative assertions; cache-transition tests | Automated two-tenant release tests prove isolation for direct URLs, APIs, cache transitions, exports, files, messages, search, notifications, and mobile. |
| G19 | Mobile notifications mostly do not deep-link | Mobile | High · L-B · P1 | `In progress` | Typed notification registry; tenant context; route tests | Every actionable type resolves to the authorized tenant, screen, record, and action; unknown types fall back to evidence detail; route tests cover allow and deny paths. |
| G20 | Owner next-action fields are strong but outcomes are weak | Sales | Medium · L-C · P2 | `In progress` | Action completion evidence; forecasting; call/reply handoffs | Strong owner/action/due invariants remain; completion is recorded; conversion cohorts show whether actions improve replies, calls, qualified opportunities, and wins. |

## Dependency map

```text
G03 -> G04 -> G01 -> G02
             ├-> G07
             └-> accepted department handoffs

G20 -> G06 -> G05 -> G11 -> G12 -> G13

G08 -> G09 -> G10

G14 -> G15 -> G16

G17 -> G18 -> G19
```

Cross-cutting release-safety work in the Phase 0 baseline—credential containment, migration safety, build isolation, configuration validation, recovery, and required CI—remains a prerequisite for any production activation even though it is not renumbered into the canonical G01–G20 product-capability set.

## Current implementation evidence

| Evidence | Supports | Result |
|---|---|---|
| Root regression run on implementation branch | Cross-cutting safety | 829 passed, 0 failed locally and in GitHub Actions on 2026-07-24; the repository CI gate also passed secret scanning and real PostgreSQL migration execution on exact implementation commit `c8f54d5`. |
| Handler registry inventory | G03 | 63 handler files and registrations; 51 uniquely scheduled names and 12 event/on-demand handlers. Machinery breadth does not prove outcomes. |
| Aggregate operating evidence | G03, G08–G10, G20 | 8,732 jobs, one reply, no meetings; 15 content items posted and 15 rejected. Outcome readiness remains unproven. |
| Static repository and migration audit | Cross-cutting safety | Migration, schema, configuration, container, and recovery blockers remain open as documented in the baseline. |
| Safety workflow on this branch | Cross-cutting safety | Adds clean install, root tests, and `npm run security:secrets`; verification requires the script, a passing workflow, and required branch protection. |
| Production activation boundary on this branch | G02 and all Head activations | Defines change classes, approval, evidence, rollback, and observation requirements; operational adoption is still required. |
| `067_agent_job_outcomes.sql` plus recorder contract | G03 | Separates execution, result, output, quality, delivery, and business outcome. Application recording is best-effort and the migration remains unapplied. |
| `068_work_items_control_plane.sql`, `072_work_item_atomic_rpcs.sql`, `073_work_items_read_compatibility.sql`, pure planners, and gated read API | G01, G07, G17, G18 | Adds a tenant-bound, append-only ledger, atomic service-role command boundary, optimistic transitions, deployed owner-role compatibility, deterministic grants, and an explicit-projection API restricted by a global flag plus exact tenant cohort. Writes and production migration remain disabled. |
| `069_document_control.sql`, `080_document_ingestion_retrieval.sql`, access/ingestion contracts, gated read/search API, and light-first web/mobile Document Center surfaces | Document-management requirement, G17, G18 | Adds private canonical metadata, immutable versions and ingestion receipts, tenant-scoped SHA-256 duplicate detection, search chunks with guarded version-specific citations, links, audit events, explicit user/role/agent grants, classification-aware RLS, safe storage-path calculation, permissioned retrieval, and read-only responsive interfaces. The web surface rechecks every response against the authenticated `app_metadata` tenant and the mobile service rejects any mismatched document, version, event, link, or search projection before render. Supabase Storage is the confirmed current provider contract, but provider dispatch is structurally disabled. Upload dispatch, malware/extraction workers, version promotion, signed retrieval, live three-client evidence, and production migration/activation remain gated. Web commit `61cbc7a` and mobile commits `ef00249`/`73f97e7` are isolated on implementation branches; all 23 mobile notification, Document Center, and Department Center safety tests pass and both iOS and Android Expo exports compile. Figma frame `30:10` matches the warm-light mobile implementation and is explicitly labeled as sample data so it cannot be confused with tenant evidence. |
| `070_scheduling_control.sql`, Calendly adapter, gated webhook projection, `077_calendly_appointment_projection.sql`, `078_scheduling_lifecycle_control.sql`, and the pure lifecycle planner | G06 | Adds a tenant-guarded scheduling state machine, policies, events, window validation, owner binding, and collision-resistant idempotency. Strictly verified and exactly allowlisted Calendly receipts can project booking/cancellation state through a service-role-only atomic RPC without canonical PII or raw payloads. The lifecycle layer records invitation, booking, reminder, reschedule, preparation, completion, follow-up, exceptions, provider receipts, optimistic revisions, and immutable evidence while structurally forbidding provider dispatch. The old reply-coordination decision is superseded. Production has no active Calendly integration or scheduling policy; existing Google OAuth flows have mail rather than Calendar scopes. Provider/calendar activation, runtime dispatch, evidence observation, and consolidated approval remain gated. |
| `071_referral_tenant_integrity.sql` plus unconditional route validation | G18, G20 | Blocks new cross-tenant referral relationships at both API and database boundaries. Migration remains unapplied. |
| `074_incident_recovery_reconciliation.sql`, typed planner, and gated Operations Guardian adapter | G04, G01, G03 | Resolves recovery citations against tenant-bound authoritative `agent_jobs` or prospecting `leads` evidence, then atomically links canonical work, verifies it, recovers the incident, supersedes its attention row, and appends immutable evidence. The Guardian uses this path only when three write flags and three exact tenant cohorts agree; failure cannot fall back to a direct recovery update. The required short-window production observation remains unproven. |
| `075_work_item_identity_hardening.sql` plus command-boundary validation | G01, G17, G18 | Requires current tenant-owner membership for human-created work and human events, tenant membership for new human assignment, and tenant ownership for new supported entity links. It fails closed for new agent/service identities until registries exist, preserves updates to unchanged legacy fields, and forces service mutations through atomic RPCs. HTTP commands reject direct relationships and assignment spoofing; RPC results require a complete typed contract and explicit projection. |
| `076_closed_won_onboarding_handoff.sql`, typed planner, Stripe adapter, and evidence-preserving rollback | G05, G11, G17, G18 | Adds distinct source-sales and client-onboarding tenant identities, authoritative won/customer/workflow checks, one immutable source event, owner or supervised-service acceptance, workflow acknowledgment, SLA/retry/exception states, optimistic revisions, and service-role-only default-off commands. The verified Stripe checkout adapter requires three flags, separate exact source/client cohorts, immutable UUID metadata, and PII-free digest evidence; retries resume from committed state. PostgreSQL negative tests cover disabled/authenticated/direct-write/cross-tenant/system-spoof denial, replay, lifecycle advancement, service acceptance, acknowledgment, and completion. Production activation remains disabled. |
| Ephemeral PostgreSQL CI harness | G01, G02, G04–G09, G12, G15–G20 and cross-cutting safety | Applies and reapplies migrations 067–092; proves atomic command denial/replay/conflict/cross-tenant behavior; exercises role/classification document RLS, ingestion/retrieval citations, incident reconciliation, closed-won onboarding, appointment projection/lifecycle, billing attribution, monthly finance close, content delivery/quality, client-health interventions, lead-action outcomes, all seven supervised Department Heads, and Chief-of-Staff dependency/authority gates; upgrades a simulated released migration-068 database through 092; and executes evidence-preserving containment rollbacks in reverse dependency order. The complete hardened 067–092 path, all 18 database proof suites, Finance and Marketing two-session command/containment ordering, replay, and reverse 092→085 rollback pass locally in one disposable PostgreSQL 16 instance. GitHub Actions runs [30121857287](https://github.com/patricksjenkins/growth-os/actions/runs/30121857287) and [30121855574](https://github.com/patricksjenkins/growth-os/actions/runs/30121855574) independently passed the same safety workflow through 092 on exact implementation commit `c8f54d5`. |
| Webhook route manifest/readiness and strict handler gates | Cross-cutting safety | Every mounted provider remains a strict-mode requirement even if labelled legacy or retired; strict mode fails closed while default-off compatibility preserves production behavior. |
| Shared action authority contract | G01, G02, G17, G18 | Tenant, role, identity type, and action are evaluated in one fail-closed contract for the new work and document surfaces. Every production-boundary action is represented as approval-required and can never be granted by this evaluator. Existing route-by-route convergence remains in progress. |
| Canonical integer finance calculation contract | G14, G15, G16 | Calculates exact minor-unit totals, preserves authority and attribution as separate dimensions, reports missing evidence as unknown, and blocks close/signoff for any difference or incomplete evidence. It is deliberately shadow-only and not yet wired to client-visible totals. |
| `079_billing_customer_attribution.sql`, pure planner, and negative database proof | G15, G17, G18 | Adds immutable authoritative provider identity mappings and exact signed minor-unit customer revenue/cost/margin attribution. Provider identities and source events cannot be rebound across tenants; direct DML is denied; service RPCs are default-off and reconciliation fails closed. Stripe runtime ingestion, authoritative metadata conventions, tenant cohort, cost/currency sources, and shadow evidence remain activation gates. |
| `081_monthly_finance_close_control.sql`, pure planner, and negative database proof | G14, G15, G16, G17, G18 | Adds an exact tenant/month/currency close cycle with reconciliation prerequisites backed by migration 079 evidence, durable exceptions and SLA work, human review, separate owner sign-off, immutable events, optimistic revisions, idempotency, containment rollback, and a one-way kill switch. Provider export and production period locking are structurally false; the code can only record evidence of an independently produced artifact and a shadow lock decision. PostgreSQL execution, evidence observation, and activation remain gated. |
| `082_content_delivery_quality_control.sql`, pure planners, and negative database proof | G03, G08, G09, G17, G18 | Separates execution, output, calibrated quality, provider delivery, retry/stuck/exception state, and business effect in immutable tenant-bound receipts. Completed no-op/no-output work cannot appear delivered or effective; accepted quality is bound to versioned rubrics and calibration; failed delivery requires owner work. Provider dispatch is structurally false, the feature is default-off, and the one-way kill switch atomically contains writes. External delivery reconciliation, calibration observation, and activation remain gated. |
| `083_client_health_interventions.sql`, pure planner, and negative database proof | G03, G12, G17, G18 | Adds exact tenant/customer signal snapshots, durable intervention ownership, acceptance, SLA, escalation, completion, and observed outcome receipts. Heuristics cannot assert stable health or prove outcomes; completed actions remain unproven until a client outcome receipt exists. Communications/provider actions are structurally false, activation requires a same-tenant privileged human, and the one-way kill switch is service-only. Backtesting, live outcome observation, and activation remain gated. |
| `084_lead_action_evidence_cohorts.sql`, pure planner, and negative database proof | G03, G20, G17, G18 | Adds exact tenant/lead/action identity, immutable assignment-through-outcome receipts, optimistic revisions, idempotency, and honest cohorts that separate observed, unknown, and pending results without claiming causality. Outreach/provider dispatch is structurally false; recursive PII/provider/causal payloads are rejected; the kill switch stores only a reason digest. Live cohort observation and activation remain gated. |
| Executable seven-Department catalog, generic supervised planners, read-only `/api/departments`, and light-first web/mobile Department Centers | G02, G03, G17, G18 and Department layer | Defines one canonical mission, at least five measurable KPIs, accepted report types, supervised actions, owner-approval actions, and prohibitions for Reliability, Revenue, Onboarding, Client Success, Finance, Marketing, and Product/Engineering. Unknown actions fail closed; production, customer contact, money, pricing, legal, advertising, and release actions can never receive planner authority. The API is hidden behind a global flag and exact tenant cohort, uses JWT-bound RLS reads, revalidates owner authority and tenant identity, and exposes no mutation route. The light web Department Center in site commit `eb7e828` and platform-owner mobile Department Center in app commit `73f97e7` consume only these contracts and tenant-bound report evidence, visibly report production authority as zero, and expose no activation control. The mobile service rejects mismatched catalog, report, contract, or detail projections before render. Figma frames `27:87` and `29:10` match the verified warm-ivory, navy, green, burgundy, and amber implementation and are explicitly labeled as synthetic evidence previews. Domain-specific durable implementations now exist for all seven Heads; production authority and evidence-period gates remain closed. |
| `085_reliability_department_head.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G03, G04, G17, G18 and Reliability Head | Adds a real supervised Reliability/Security/Agent Operations Head with a written mission, six KPI contracts, immutable structured reports, separate execution/outcome health, domain-complete evidence before healthy state, durable goals/work/recommendations/exceptions, assignment/acceptance/SLA/escalation/completion/outcome contracts, exact registered Head and tenant-role authority, idempotency, optimistic revisions, RLS, immutable audit, and a one-way kill switch. Operational writes, provider dispatch, customer communication, production authority, and money movement are structurally false. Focused tests pass 15/15; shared disposable PostgreSQL apply, replay, tenant negatives, and rollback retain five events and two reports. Two weeks of supervised operating evidence remain unproven. |
| `086_revenue_department_head_supervised.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G05, G06, G20, G17, G18 and Revenue Head | Adds a real supervised Revenue/Sales Head with an owner-approved mission/KPI charter, evidence-manifested funnel reports, database-derived qualification-through-win health, durable goals/work/decisions/exceptions, assignment/acceptance/start/SLA/escalation/completion contracts, exact registered Head and tenant-owner authority, idempotency, RLS, immutable audit, source anti-rebinding, and a one-way kill switch. Completed work cannot make funnel outcomes green. Outreach, provider, pricing, production, and financial authority are structurally false. Focused tests pass 16/16; shared disposable PostgreSQL apply, replay, tenant negatives, and rollback retain nine events, three work items, two reports, and two charters. Repeatable qualified-reply/demo evidence and two weeks of accepted reporting remain unproven. |
| `087_chief_of_staff_supervised_control.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G01, G17, G18 | Adds a genuine supervised Chief of Staff control layer for company goals/KPIs, exact dependency links, capacity conflicts, decision-required items, exceptions, and follow-through assignment/acceptance/SLA/escalation/completion. Every coordination command revalidates owner-accepted versioned interoperability contracts plus real accepted 085 Reliability and 086 Revenue reports for the exact reporting window. Tenant membership and actual database role must match claimed authority. Production writes, providers, customer communications, and financial actions are structurally false; audit is immutable; the kill switch is one-way and stores only a reason digest. Focused tests pass 12/12; shared disposable PostgreSQL proof passes ordered apply, dependency gates, two-tenant negatives, and reverse rollback while retaining five events. Activation and the two-week report gate remain unproven. |
| `088_onboarding_department_head_supervised.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G05, G11, G17, G18 and Onboarding Head | Adds a genuine supervised Onboarding/Implementation Head bound to exact authoritative closed-won handoff and onboarding-workflow evidence, with mission/KPIs, structured reports, durable goals/work/decisions/exceptions, assignment/acceptance/SLA/escalation/completion/outcome contracts, exact tenant/client/role/registered-Head authority, revisions, semantic idempotency, RLS, immutable evidence, and a one-way kill switch. Implementation completion remains outcome-unproven until a separate immutable owner-verified canonical outcome receipt exists; Head, agent, service, and system identities cannot self-certify it. Strict minimized evidence rejects normalized sensitive-key variants. Provider dispatch, customer communications, production writes, and money authority are structurally false. Focused tests pass 20/20; disposable PostgreSQL apply through 092, tenant negatives, synthetic-outcome and sensitive-field regressions, replay, and rollback pass while retaining one receipt, one report, two cases, and five events. Live three-client evidence and the observation period remain unproven. |
| `089_client_success_department_head_supervised.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G12, G17, G18 and Client Success Head | Adds a genuine supervised Client Success/Support Head grounded in authoritative client-health, intervention, and immutable canonical support-snapshot evidence, with mission/KPIs, structured reports, durable goals/work/decisions/exceptions, assignment/acceptance/SLA/escalation/completion/outcome contracts, exact tenant/role/registered-Head authority, revisions, semantic idempotency, RLS, immutable evidence, and a one-way kill switch. Unverified support snapshots force service health to unverified; completed work and interventions remain unproven until a separate observed outcome exists. Customer communications, provider dispatch, production writes, finance, refunds, and credits are structurally false. Focused tests pass 17/17; disposable PostgreSQL apply through 092, two-tenant negatives, false-green and exact-Head proofs, replay, kill, and rollback pass while retaining five snapshots, five reports, and three events. Live intervention outcomes and the observation period remain unproven. |
| `090_finance_data_governance_head_supervised.sql`, planner, runtime and concurrency proofs, and evidence-preserving rollback | G02, G14–G18 and Finance Head | Adds a genuine supervised Finance/Data Governance Head grounded in exact canonical integer attribution, reconciliation, monthly-close, and governance evidence, with mission/KPIs, structured reports, durable goal/work/decision/exception terminal lifecycles, exact stored-assignee authority, revisions, semantic idempotency, RLS, immutable evidence, shared control-row serialization, and a one-way kill switch. Successful execution cannot imply reconciled financial truth; verification requires the complete canonical attribution set and a shadow-locked close. Strict minimized evidence rejects normalized sensitive-key variants. Money movement, charges/refunds, pricing, provider dispatch, period locking, exports, production writes, and customer communications are structurally false. Focused tests pass 18/18; disposable PostgreSQL apply through 092, existing finance containment suites, two-tenant negatives, real two-session command/containment ordering, replay, kill, and rollback pass while retaining two reports, seven cases, and 17 events. Live close observation and production financial authority remain prohibited. |
| `091_marketing_brand_head_supervised.sql`, planner, runtime/concurrency proofs, and evidence-preserving rollback | G02, G08, G09, G17, G18 and Marketing Head | Adds a genuine supervised Marketing/Brand Head grounded in immutable content artifact, calibrated quality, delivery, and observed-effect evidence, with mission/KPIs, bounded descriptive cohorts, complete durable goal/work/decision/exception lifecycles, exact tenant/role/registered-Head-owned work, assignment/acceptance/SLA/escalation/completion/outcome, revisions, idempotency, RLS, immutable audit, control-row serialization, strict normalized/minimal metadata, and one-way kill. Completed execution/content cannot imply quality, delivery, causality, or business effect. Publication, providers, customer contact, paid advertising, spend, pricing/legal changes, and production writes are structurally false. Focused tests pass 20/20; combined 067–092 apply/replay, migration-082 compatibility, tenant/authority/false-green and two-session ordering proofs, and rollback pass while retaining two reports, five cases, and 14 events. Live content outcome evidence and the observation period remain unproven. |
| `092_product_engineering_head_supervised.sql`, planner, runtime negative proof, and evidence-preserving rollback | G02, G03, G04, G17, G18 and Product/Engineering Head | Adds a genuine supervised Product/Engineering Head with six evidence-specific report contracts covering quality, throughput, tenant isolation/regression, incident/rollback readiness, accessibility/security debt, and verified product outcomes. Durable goals/work/decisions/exceptions, exact tenant/role/registered-Head authority, assignment/acceptance/SLA/escalation/completion/outcome, revisions, idempotency, RLS, immutable audit, strict normalized/minimal metadata, and one-way kill are enforced. Engineering completion remains outcome-unproven until an immutable canonical product-outcome receipt is verified by an independent same-tenant owner distinct from the registered Head; service role cannot impersonate that verifier. Reports and work outcomes retain exact receipt bindings. Code merge, deployment, migration application, feature activation, TestFlight/App Store release, provider/customer action, money, pricing, and legal authority are structurally false. Focused tests pass 21/21; combined 067–092 apply/replay, owner/service/Head receipt-identity regressions, base tenant and domain proofs, and rollback pass while retaining one receipt and two reports. Production authority and the evidence period remain gated. |
| Tenant-bound push envelope and mobile registry | G17, G18, G19 | The active Expo sender injects the exact recipient tenant before device lookup, rejects conflicting tenant claims before any provider call, and queue dispatch converts only approved category/entity identifiers into routing data. The mobile resolver accepts 16 typed contracts and fails closed for stale tenants, wrong users, arbitrary routes, malformed identifiers, signed-out state, and unknown types. Direct legacy producers retain their existing title/body behavior; end-to-end provider delivery and three-client device-state tests remain gated. |
| Canonical Telnyx readiness and active-SMS regression contract | Cross-cutting communications safety | New outbound producers authorize only a tenant-bound Telnyx number plus the platform Telnyx API capability. Legacy Twilio rows remain available only to historical mounted paths and cannot grant a new send. Provisioning writes canonical Telnyx keys without deleting historical configuration. |

## Canonical activation roadmap

| Phase | Exact title | Entry dependency | Exit gate |
|---:|---|---|---|
| 0 | Reliability foundation | First; no new production behavior required for a read-only reliability role | Two consecutive weeks with no undetected zero-output condition; every critical incident has diagnosis, owner, SLA, and verification. |
| 1 | Revenue remediation, then Head of Revenue | Phase 0 success; canonical funnel; department reports store | Repeatable qualified-reply and demo signal; the Head reports KPIs, assigns accepted work, and escalates exceptions. |
| 2 | Chief of Staff | Revenue Head has delivered decision-ready reports for two weeks | The owner receives only decisions, relationship moments, commitments, and high-risk escalations. |
| 3 | Onboarding Head | At least one accepted new-customer handoff; checklist and acceptance contract | No step stalls silently; every human action is scheduled with context. |
| 4 | Client Success Head | Stable identity/routing; support knowledge base; SLA policy | SLA is measured; value evidence is delivered; a risk plan is accepted before owner escalation. |
| 5 | Finance & Data Governance Head | Reconciled metric contracts and close checklist | Every executive metric reconciles to source; monthly close becomes review rather than repair. |
| 6 | Marketing Head | Routine content rejection below 10% for eight weeks; case-study evidence; revenue attribution | Content delivers accepted proof and attributable opportunities. |
| 7 | Product & Engineering Head | Structured feedback, release evidence, rollback, and incident contracts | The Head coordinates planning and QA; deploy and migration authority remains human. |

The roadmap is sequentially constrained, not calendar-promised. A later phase does not activate because code exists or time has elapsed.

## Gate-change protocol

Every status change must add:

1. the commit, artifact, or runbook reference;
2. the validation command or review record;
3. the environment and date;
4. aggregate results with no secrets or customer-level data;
5. the accountable reviewer;
6. known residual risk;
7. rollback or containment evidence.

Statuses must not be advanced by editing this table alone. If evidence later regresses, change the status back to `Blocked` or `Partial` and record the reason in the implementing pull request or incident record.
